= image_tag tweet.profile_image_url, :class => "twitter_profile", :alt => tweet.from_user
%strong.from_user= tweet.from_user
= twitify(tweet.text)
= link_to "#{time_ago_in_words(tweet.created_at)} ago", tweet_link(tweet), :class => "when"