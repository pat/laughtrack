<div class="page-header">
  <h1>Unclassified Tweets</h1>
</div>

<ul class="nav nav-tabs">
  <li><%= link_to "Pending (#{pending_tweets.count})", [:manage, :tweets] %></li>
  <li class="active"><%= link_to "Unclassified (#{unclassified_tweets.count})", [:unclassified, :manage, :tweets] %></li>
  <li><%= link_to "Detached (#{detached_tweets.count})", [:detached, :manage, :tweets] %></li>
</ul>

<%= form_tag [:bulk_update, :manage, :tweets], :method => :put do %>
  <div class="form-actions">
    <button type="submit" class="btn btn-primary">Save Changes</button>
  </div>

  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Who</th>
        <th>What</th>
        <th>When</th>
        <th>Wait</th>
        <th>Positive</th>
        <th>Negative</th>
      </tr>
    </thead>
    <tbody>
      <% unclassified_tweets.each do |tweet| %>
      <tr>
        <%= render 'tweet_cells', :tweet => tweet %>
        <td><%= radio_button_tag("action_#{tweet.id}", "wait", true) %></td>
        <td><%= radio_button_tag("action_#{tweet.id}", "positive", false) %></td>
        <td><%= radio_button_tag("action_#{tweet.id}", "negative") %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <div class="form-actions">
    <button type="submit" class="btn btn-primary">Save Changes</button>
  </div>
<% end %>
