%table.unclassified
  %thead
    %tr
      %th Tweet
      %th Classification
  %tbody
    - @docs.each do |doc|
      %tr
        %td
          = doc.text
          = link_to Show.find(doc.show_id).name, edit_admin_show_path(doc.show_id), :class => 'show'
        %td= doc.classification
